# DFIR Parser Releases

Public update feed for prebuilt DFIR Parser Windows releases.

This repository contains release metadata and compiled update packages only. The application source code is maintained separately in a private repository.

## Update feed

The application reads `dfir-parser-update.json` from the `main` branch. Release ZIP archives are published as GitHub Release assets.
